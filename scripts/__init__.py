"""Command-line entry points for FinAI-Qwen.

Each module here is a thin, argparse-driven wrapper around the reusable logic in
:mod:`src`. Run them as scripts from the repository root, e.g.::

    python scripts/download_model.py
    python scripts/train.py --epochs 1 --max-steps 50
"""
